# Copy to .env.local and fill in to use live odds instead of sample data.
# Get a key at https://the-odds-api.com (the free tier covers NFL futures).
ODDS_API_KEY=
# Set to "live" to fetch real odds, or leave as "sample" to use seeded demo data.
ODDS_SOURCE=sample
